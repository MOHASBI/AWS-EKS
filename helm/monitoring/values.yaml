grafana:
  ingress:
    enabled: true
    ingressClassName: nginx
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
    hosts:
      - grafana.eks.mohamedhassanproject.com
    tls:
      - hosts:
          - grafana.eks.mohamedhassanproject.com
        secretName: grafana-tls

prometheus:
  ingress:
    enabled: true
    ingressClassName: nginx
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
    hosts:
      - prometheus.eks.mohamedhassanproject.com
    tls:
      - hosts:
          - prometheus.eks.mohamedhassanproject.com
        secretName: prometheus-tls